[project]
name = "rag-search-engine"
version = "0.1.0"
description = "A RAG serch engine :)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "nltk==3.9.1",
    "numpy>=2.4.3",
    "sentence-transformers>=5.3.0",
]
